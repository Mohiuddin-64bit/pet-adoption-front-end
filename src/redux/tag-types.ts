export enum tagTypes {
  adoption = "adoption",
  contacts = "contacts",
  profile = "profile",
  pets = "pets",
  admin = "admin",
  user = "user",
}

export const tagTypesList = Object.values(tagTypes);